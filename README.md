This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```
## Features

The Site generates HTML output. 
About page includes how to use video.
Nav Bar works.
Hamburger Menu works.
Darkmode is implemented

## Issues
Dark/Light mode switches correctly, however it does not effect the components on the page. I think this is an issue with the MUI components I imported, I believe tailwind has issue with them.